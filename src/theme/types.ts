/** Preferencia de tema elegida por el usuario. `system` sigue al sistema operativo. */
export type Theme = 'light' | 'dark' | 'system';

/** Tema efectivo ya resuelto (lo que realmente se pinta). */
export type ResolvedTheme = 'light' | 'dark';
